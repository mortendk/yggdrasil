{#
/**
 * @file
 * Default theme implementation to display a view of unformatted rows.
 *
 * Available variables:
 * - title: The title of this group of rows. May be empty.
 * - rows: A list of the view's row items.
 *   - attributes: The row's HTML attributes.
 *   - content: The row's content.
 *
 * @see template_preprocess_views_view_unformatted()
 *
 * @ingroup themeable
 */
#}
{% if title %}
  <h3>{{ title }}</h3>
{% endif %}


{% for delta, row in rows %}
{# would be sweet to create all the class bs here as we need it
  views-row views-row-1 views-row-odd views-row-first #}
{% set classes %}
{% spaceless %}
 count-{{ loop.index }}
 {{ cycle(["even", "odd"], delta) }}
 {% if loop.first %} the-first{% endif %}
 {% if loop.last %} the-last{% endif %}
{% endspaceless %}
{% endset %}

  <div class="{{ classes|replace({'\n':''}) }}" {{ row.attributes }}>
    {{ row.content }}
  </div>
{% endfor %}
